{
  "type": "postgres",
  "host": "localhost",
  "port": 5000,
  "database": "postgres",
  "username": "root",
  "password": "root",
  "logging": false,

  "entities": [
    "apps/**/*.entity.ts",
    "libs/**/*.entity.ts"
  ],
  "migrations": ["db/migrations/*.ts"],

  "cli": {
    "migrationsDir": "db/migrations"
  }
}
