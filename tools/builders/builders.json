{
  "builders": {
    "db-migrate": {
      "implementation": "./db-migrate",
      "schema": "./db-migrate/schema.json",
      "description": "Runs typeorm's migrations."
    },
    "gen-migration": {
      "implementation": "./gen-migration",
      "schema": "./gen-migration/schema.json",
      "description": "Generates typeorm's migrations."
    }
  }
}
