{
  "builders": {
    "deploy": {
      "implementation": "./deploy",
      "schema": "./deploy/schema.json",
      "description": "Deploys project to heroku."
    },
    "db-migrate": {
      "implementation": "./db-migrate",
      "schema": "./db-migrate/schema.json",
      "description": "Runs typeorm's migrations."
    },
    "tsc-build": {
      "implementation": "./tsc-build",
      "schema": "./tsc-build/schema.json",
      "description": "Build typescript code with tsc."
    }
  }
}
