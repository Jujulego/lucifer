{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "buildPath": {
      "type": "string",
      "description": "Path containing built code to deploy."
    },
    "repository": {
      "type": "string",
      "description": "Target repository."
    }
  },
  "required": ["buildPath", "repository"]
}
